name: sf_env
channels:
  - snowflake
dependencies: []
# NOTE: streamlit= version pin has no effect in Snowflake Native Apps.
# The Streamlit runtime is bundled by Snowflake in the container layer
# and is NOT controlled by environment.yml. Add extra packages here as needed
# (e.g. pandas, plotly) but do not specify streamlit=.
